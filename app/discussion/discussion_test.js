'use strict';

describe('myApp.discussion module', function() {

  beforeEach(module('myApp.discussion'));

  describe('discussion controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var discussionCtrl = $controller('discussionCtrl');
      expect(discussionCtrl).toBeDefined();
    }));

  });
});